snapshots:
  - name: orders_snapshot_check
    relation: source('jaffle_shop', 'orders')
    config:
      database: analytics   # 明确指定 database
      unique_key: id
      strategy: check
      check_cols:
        - status